function showWelcomeMessage(welcome) {
    if (welcome) {
        alert("회원가입을 환영합니다!");
    }
}

function showDeleteMessage(deleteMessage) {
    if (deleteMessage) {
        alert(deleteMessage);
    }
}

function showWriteMessage(writeMessage) {
    if (writeMessage) {
        alert(writeMessage);
    }
}